
const names = (["Guadalupe", "Ollie", "Aki"]);

const message = []; 

function writeCards(names, message){
    for (let i=0; i < names.length; i++){
        return `Thank you, ${names}, for the wonderful surprise gift!`;
        
    }
}

